1.Create a ML project floder in pc/laptop
2.Create  a virtual environment in that folder
   Using python -m venv folder_name
3.Activate the enviroment
   To activate,we use .\folder_name\scripts\activate
4.Create a backend folder, frontend folder and Readme.md file.
5.In backend folder,we create data folder,model folder,requriments.txt,app.py and pickle file train_save.py
6. In frontend folder,we create app.py and requriments.txt 

# Inside data,we create a file load_data.py and in model created a file train_model.py
# After creating the fileswe created train_save.py and loaded the requriments.txtx
# To load libaries we use pip install -r requriments.txt
# After this we ran our train_save.py which created a pkl file

# Run the app.py

# start another terminal to use the files related to frontend
# Again we have activate the enviroment
In frontend we created requriments.txt and app.py
# before we run we will load the libaries which are requried in the frontend folder.
# after loading he libaries we ran the app.py infrontend through streamlit 
# Torun streamlit we use streamlit run app.py


# Push to github
git  init
git add backend/frontend/readme.md

